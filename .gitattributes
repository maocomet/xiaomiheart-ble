# Normalise to LF in the repository.
* text=auto

# The Gradle wrapper scripts must keep their native line endings: CI runs the
# shell script on Linux, where CRLF produces "bad interpreter: No such file".
gradlew text eol=lf
gradlew.bat text eol=crlf

*.jar binary
*.apk binary
